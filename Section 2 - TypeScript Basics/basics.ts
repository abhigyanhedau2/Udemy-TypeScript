function sum(num1: number, num2: number, showResult: boolean): number {
    if (showResult) return num1 + num2;
}

const num1: number = 5;
const num2: number = 2.8;
const printResult: boolean = true;


console.log(sum(num1, num2, printResult));
